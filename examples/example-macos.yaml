name: "Setup YQ Example - MacOS"

on:
  workflow_dispatch:
    inputs:
      version:
        type: string
        required: false
        description: "Version of YQ to install"
      download-compressed:
        type: boolean
        required: false
        description: "Download .tar.gz of binary rather than raw binary."
        default: true
      force:
        type: boolean
        required: false
        description: "Do not check for existing yq installation before continuing."
        default: false

permissions: {}

jobs:
  example:
    runs-on: macos-latest
    steps:
      - name: 'Setup yq'
        uses: step-security/install-yq-action@v1
        with:
          version: '${{ inputs.version }}'
          download-compressed: '${{ inputs.download-compressed }}'
          force: '${{ inputs.force }}'

      - name: 'Check yq'
        run: |
          which yq
          yq --version
